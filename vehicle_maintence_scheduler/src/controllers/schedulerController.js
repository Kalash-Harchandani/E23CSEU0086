export const getDepots = async (
    req,
    res
) => {

    res.status(200).json({
        message: "Get depots endpoint working"
    });

};

export const getVehicles = async (
    req,
    res
) => {

    res.status(200).json({
        message: "Get vehicles endpoint working"
    });

};

export const generateSchedule = async (
    req,
    res
) => {

    res.status(200).json({
        message: "Generate schedule endpoint working"
    });

};